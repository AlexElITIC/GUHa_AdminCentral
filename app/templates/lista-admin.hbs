<div class="container">
<div class="row">
    <div class="col s12">
      <div class="row">
        <div class="input-field col s9">
          <i class="material-icons prefix">search</i>
          <input type="text" id="autocomplete-input" class="autocomplete">
          <label for="autocomplete-input">Busqueda de administrador</label>
        </div>
        <div class="input-field col s3">
<a class="waves-effect waves-light btn #0091ea light-blue accent-4"><i class="material-icons left">send</i>buscar</a>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="container">
  <div class="row ">
    {{#each listaAdmin as |admin|}}
    {{#if admin.nombre}}
    {{tarjeta-administrador administrator=admin}}
    {{/if}}
    {{else}}
    <h2>no hay registros :(</h2>
    {{/each}}
  </div>
</div>
